# converted model weights — multi-GB, regenerate with convert_model.py
models/

.venv/
__pycache__/
*.pyc
.DS_Store

# NOT ignored: .scratch/ is this project's issue tracker (see
# docs/agents/issue-tracker.md), so it belongs in version control.

# uploaded/test clips
audio/*.wav
# per-machine model list (models.local.json is your own additions, not the project's)
models.local.json
# per-machine station config: device names differ on every host, so this is
# deployment state, not project state. deploy/stations.example.json is the
# committed starting point.
stations.json
stations.json.tmp
